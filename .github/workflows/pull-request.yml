name: Swift/WinRT PR Build & Test

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    uses: ./.github/workflows/callable-windows-build.yml
    secrets: inherit
    with:
      config: debug
      publish-nuget: false
