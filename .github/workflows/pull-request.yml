name: Pull Request Build

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Swift/WinRT PR Build & Test
    uses: ./.github/workflows/callable-windows-build
    secrets: inherit
    with:
      config: debug
      build-nuget: false
