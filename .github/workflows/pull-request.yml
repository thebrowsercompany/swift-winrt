name: PR

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Swift/WinRT Debug
    uses: ./.github/workflows/callable-windows-build.yml
    secrets: inherit
    with:
      config: debug
      publish-nuget: false
