This is a checked-in version of the Microsoft.Windows.WinMD C++ metadata parsing nuget package.

Trying to use cmake and nuget in VS doesn't work very well and provides a significantly worse
developer experience, so we're just checking these in for now.
